spring:
  datasource:
    url: ${dburl} #jdbc:postgresql://localhost/sensordata?user=sensors-app&password=password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: none
